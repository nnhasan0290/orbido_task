const IconDefault = ({ src }) => (
    <div className="h-[64px] w-[64px] rounded-full icon_rgba flex items-center justify-center flex-shrink-0">
        <img src={src} alt="" />
    </div>
);

export default IconDefault;
